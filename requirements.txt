# Dépendances du projet SmartContractDevPipeline
# NOTE: python>=3.9 n'est pas une dépendance pip, c'est une exigence système

# Core
PyYAML>=6.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Async
aiohttp>=3.9.0

# Web3 & Blockchain
web3>=6.0.0
eth-account>=0.11.0
eth-typing>=3.0.0

# Development (optionnel)
black>=23.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0

# API (optionnel)
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0

# Utils (optionnel)
jinja2>=3.1.0
rich>=13.0.0
